{
  "name": "devmultiplier-academy",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter @devmultiplier/web dev",
    "build": "bun run --filter @devmultiplier/web build"
  }
}
